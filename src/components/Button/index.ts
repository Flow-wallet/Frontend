import { BaseButton } from "./BaseButton";
import { IconButton } from "./IconButton";
import { LoginConnectionButton } from "./LogginConnectionButton";

export const Button = {
  Icon: IconButton,
  //   Toggle: ToggleButton,
  Base: BaseButton,
  LoginConnection: LoginConnectionButton,
};
