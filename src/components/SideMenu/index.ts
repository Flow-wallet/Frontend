import { SideMenuBrand } from "./Brand";
import { SideMenuButton } from "./Button";
import { SideMenuRoot } from "./Root";

export const SideMenu = {
  Root: SideMenuRoot,
  Brand: SideMenuBrand,
  Button: SideMenuButton,
};
